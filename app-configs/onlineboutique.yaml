manifest_path: release/kubernetes-manifests.yaml
namespace: onlineboutique

exclude_resource_patterns:
  - "(^|/)loadgenerator($|[-/])"
  - "(^|/)prometheus($|[-/])"
  - "(^|/)grafana($|[-/])"
  - "(^|/)jaeger($|[-/])"
  - "(^|/)zipkin($|[-/])"
